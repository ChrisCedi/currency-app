import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  body: {
    paddingHorizontal: 20,
  },
  mainImage: {
    width: '100%',
    height: 150,
    paddingTop: 10,
  },
});

export {styles};
