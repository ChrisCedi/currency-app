import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'secondary',
    paddingTop: 18,
    paddingBottom: 30,
  },
  title: {
    fontWeight: 500,
    textAlign: 'center',
  },
});

export {styles};
